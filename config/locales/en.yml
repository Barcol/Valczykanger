en:
  activerecord:
    model:
      melange:
        one: Event
        few: Events
        many: Events
        other: Events
    errors:
      models:
        user:
          attributes:
            email:
              blank: cannot be empty.
            password:
              blank: cannot be empty.
            password_confirmation:
              confirmation: has to match password.

  activeview:
    melanges:
      new: Create event